# Highlight Plus Integration

导入此示例前，请先安装：

- Highlight Plus
- Event Relays (`com.cowart.event-relays`)

导入后，`InteractionExtensions` 提供 `ExtSetHighlight` 和 `ExtBindPointerDown` 扩展方法。

Highlight Plus 缺失时，导入的脚本会因为找不到 `HighlightPlus` 命名空间而产生编译错误。
